# Python
__pycache__/
*.pyc
.venv/

# estado da fila / temporarios de processamento (nunca versionar)
/.tmp-downloads/
.fila_pendente.json
.fila_pendente.tmp

# front (Vite/React)
/front/node_modules/
/front/dist/
*.tsbuildinfo

# servidor de PO token, baixado e compilado na primeira execucao
/vendor/

# videos baixados (nunca versionar)
/videos baixados/

# env / segredos
.env
.env.*
!.env.example
!front/.env.example

# SO / editor
.DS_Store
*.pem
